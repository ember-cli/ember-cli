'use strict';

module.exports = {
  name: 'Ember Random Addon',

  init: function() {
    this.treePaths['bower'] = 'bower';
  },

  included: function(app) {
    this._super.included(app);

    app.import('vendor/ember-random-addon/file-to-import.txt', { destDir: 'assets' });
    app.import(app.bowerDirectory + '/some-random-asset/file-to-import2.txt', { destDir: 'assets' });
  }
};
