'use strict';

var path = require('path');
var fs   = require('fs');

function EmberYagni(project) {
  this.project = project;
  this.name = 'Ember Yagni';
}

function unwatchedTree(dir){
  return {read: function() { return dir; }};
}

EmberYagni.prototype.treeFor = function treeFor(name) {
  var treePath = path.join(__dirname, name);

  if (fs.existsSync(treePath)) {
    return unwatchedTree(treePath);
  }
};

EmberYagni.prototype.included = function included(app) {
  this.app = app;
};

module.exports = EmberYagni;
