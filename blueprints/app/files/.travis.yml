---
language: node_js
node_js:
  - "iojs"

sudo: false

cache:
  directories:
    - node_modules

before_install:
  - "npm config set spin false"

install:
  - npm install -g bower
  - npm install
  - bower install

script:
  - npm test
